<!--
    <UserControl x:Class="EnterpriseInventory.WPF.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <TextBlock Text="Products Page"
                   FontSize="30"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
    </Grid>

</UserControl>
-->    
<UserControl x:Class="EnterpriseInventory.WPF.Views.ProductsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Product Management"
               FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal"
                Margin="0,0,0,15">
            
        <TextBox Width="300" Height="35" Padding="10" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 VerticalContentAlignment="Center"
                 Margin="0,0,10,0"/>

            <Button Content="Refresh"
                Command="{Binding UpdateProductCommand}"
                Width="100"
                Height="35"
                Background="#007bff"
                Foreground="White"
                Margin="0,0,10,0"/>

            <Button Content="Delete"
                Command="{Binding DeleteProductCommand}"
                Width="100"
                Height="35"
                Background="#dc3545"
                Foreground="White"/>
        </StackPanel>

        <Border Grid.Row="2"
            Background="#f8f9fa"
            Padding="20"
            CornerRadius="5"
            Margin="0,0,0,15">

            <StackPanel>
                <TextBlock Text="Add Product"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="0,0,0,15"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Name"/>
                        <TextBox Text="{Binding NewName}" Height="30"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                        <TextBlock Text="SKU"/>
                        <TextBox Text="{Binding NewSKU}" Height="30"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="0,0,10,0">
                        <TextBlock Text="Category"/>
                        <TextBox Text="{Binding NewCategory}" Height="30"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3" Margin="0,0,10,0">
                        <TextBlock Text="Price"/>
                        <TextBox Text="{Binding NewPrice}" Height="30"/>
                    </StackPanel>
                    <StackPanel Grid.Column="4" Margin="0,0,10,0">
                        <TextBlock Text="Quantity"/>
                        <TextBox Text="{Binding NewQuantity}" Height="30"/>
                    </StackPanel>
                    <StackPanel Grid.Column="5" Margin="0,0,10,0">
                        <TextBlock Text="Min Stock"/>
                        <TextBox Text="{Binding NewMinStockLevel}" Height="30"/>
                    </StackPanel>
                    <StackPanel Grid.Column="6"
                            VerticalAlignment="Bottom">
                        <Button Content="Add Product"
                            Command="{Binding AddProductCommand}"
                            Width="100"
                            Height="30"
                            Background="#28a745"
                            Foreground="White"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
        <DataGrid Grid.Row="3" ItemsSource="{Binding Products}"
              SelectedItem="{Binding SelectedProduct}"
              AutoGenerateColumns="False"
              CanUserAddRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="120"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Min Stock" Binding="{Binding MinStockLevel}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>