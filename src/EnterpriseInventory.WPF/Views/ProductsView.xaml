<!--
    <UserControl x:Class="EnterpriseInventory.WPF.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <TextBlock Text="Products Page"
                   FontSize="30"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
    </Grid>

</UserControl>
-->    
<Page x:Class="EnterpriseInventory.WPF.Views.ProductsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Products">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Product Management" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox Width="300" Height="35" Padding="10" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                    VerticalContentAlignment="Center" Margin="0,0,10,0"/>
            
            <Button Content="Add Product" Command="{Binding AddProductCommand}" Width="120" Height="35"
                   Background="#28a745" Foreground="White" Margin="0,0,10,0"/>
            <Button Content="Update" Command="{Binding UpdateProductCommand}" Width="100" Height="35"
                   Background="#007bff" Foreground="White" Margin="0,0,10,0"/>
            <Button Content="Delete" Command="{Binding DeleteProductCommand}" Width="100" Height="35"
                   Background="#dc3545" Foreground="White"/>
            
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}"
                 AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="120"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Min Stock" Binding="{Binding MinStockLevel}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
