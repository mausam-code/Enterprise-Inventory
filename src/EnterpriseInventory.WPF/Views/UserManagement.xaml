<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             x:Class ="EnterpriseInventory.WPF.Views.UserManagement"
             d:DesignHeight="125.503" d:DesignWidth="590.827">
<!--
    <Grid Background="#F5F5F5">
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center">
            <TextBlock Text="User Management"
                       FontSize="32"
                       FontWeight="Bold"
                       Margin="0 0 0 20"/>
            <TextBlock Text="Loading....."
                       FontSize="18"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
-->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="User Management" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"
             AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedDate, StringFormat=yyyy-MM-dd}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Background="#f8f9fa" Padding="20" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Add New User" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Username"/>
                        <TextBox Text="{Binding NewUsername}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                        <TextBlock Text="Password"/>
                        <TextBox Text="{Binding NewPassword}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="0,0,10,0">
                        <TextBlock Text="Email"/>
                        <TextBox Text="{Binding NewEmail}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Margin="0,0,10,0">
                        <TextBlock Text="Role"/>
                        <ComboBox SelectedValue="{Binding NewRole}" Height="30">
                            <ComboBoxItem Content="Admin"/>
                            <ComboBoxItem Content="User" IsSelected="True"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="4" VerticalAlignment="Bottom">
                        <Button Content="Add User" Command="{Binding AddUserCommand}" 
                           Width="100" Height="30" Background="#28a745" Foreground="White"/>
                    </StackPanel>
                </Grid>

                <Button Content="Delete Selected" Command="{Binding DeleteUserCommand}" 
                   Width="120" Height="35" Margin="0,15,0,0" Background="#dc3545" Foreground="White"
                   HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>